apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmnginx.fullname" . }}-test-connection"
  labels:
    {{- include "helmnginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmnginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
